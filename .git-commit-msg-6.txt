feat(config): reduce the wizard to 4 ConfigFields (X1 decision)

ConfigFields REMOVED (settings-only now -- the config key still works
exactly as before, just no wizard prompt): enable_reasoning_context (its
ConfigField was already gone as of commit 1), text_verbosity,
prompt_cache_key, prompt_cache_retention, enable_response_chaining
(ConfigField already gone as of commit 1).

Wizard surface is now exactly 4 fields: api_key, base_url,
reasoning_effort, enable_long_context.

Prompt text changes:
- reasoning_effort: "Select reasoning effort level" ->
  "Reasoning effort — higher is smarter, slower, costlier"
- enable_long_context: "Enable long context (>272K tokens, 2x input / 1.5x
  output pricing)" -> "Allow requests over 272K input tokens (\u22482\u00d7 cost)"

X1 decision (adjudicated): enable_long_context now carries
requires_model=True and show_when={"default_model": "contains:gpt-5.6"} --
without requires_model the field would be shown before model selection,
before default_model is even in the collected-answers dict the show_when
predicate inspects. This matches the models with modelled long rates in
_cost.py's _LONG_RATES table (gpt-5.6-sol/-terra/-luna, plus -cyber
harmless-over-inclusion); gpt-5.5 has no long-context threshold at all and
gpt-5.4-family has a threshold but no long rates (cost-neutral), so
neither belongs behind a "this costs ~2x" prompt. Does NOT touch the
5.5/5.4 capabilities table (separate work item, out of scope here).

Test delta: test_model_aware_config_gating.py's ConfigFieldGatingMetadata
and ShowWhenConsumerSimulation classes rewritten around
enable_long_context (prompt_cache_retention/text_verbosity gating tests
removed -- those fields no longer exist); test_text_verbosity.py's
ConfigField-presence test inverted to assert absence; new
tests/test_wizard_surface.py asserts exactly 4 fields with the exact
prompt strings. 796 passed, 2 deselected.

Generated with Amplifier (https://github.com/microsoft/amplifier)

Co-Authored-By: Amplifier <240397093+microsoft-amplifier@users.noreply.github.com>
